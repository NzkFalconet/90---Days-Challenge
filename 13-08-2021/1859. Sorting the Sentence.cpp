#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s = "is2 sentence4 This1 a3";
    char a='8';
    if(a>'2' && a<'5')
        cout<<"yes";
        else
    cout<<"No";
    map<int,string>m;
    vector<s>v;
    for(int i=0;i<s.size();++i)
    {
        if(s[i]!=' ')
        {
            v.push_back(s[i]);
        }
       if(s[i]==' ')
        {
            m[s[i-1]]=;
        }

    }
}
